[package]
name = "types"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = { workspace = true }

serde = { workspace = true, optional = true }
sqlx = { workspace = true, optional = true }

[features]
serde = [ "dep:serde" ]
sqlx = [ "dep:sqlx" ]
